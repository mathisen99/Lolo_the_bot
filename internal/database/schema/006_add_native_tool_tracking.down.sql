-- Rollback: Remove native tool tracking columns

-- SQLite doesn't support DROP COLUMN in older versions, 
-- so this is a placeholder for documentation.
-- To rollback, recreate the table without these columns.
